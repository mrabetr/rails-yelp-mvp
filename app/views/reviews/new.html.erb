<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit class: "btn btn-sm btn-outline-secondary" %>
<% end %>
<small>
  <%= link_to "Back", restaurant_path(@restaurant), class: "text-secondary" %>
</small>
